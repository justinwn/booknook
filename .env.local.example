# Copy to .env.local and fill in from your Supabase project settings.
# Without these the app still runs, in demo mode (no real auth/persistence).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
